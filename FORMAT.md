# SPEC format

Plain markdown. Sections: §G goal, §C constraints, §I interfaces, §V invariants, §T tasks, §B bug ledger.
Task rows use: `id|status|goal|cites`, where status is `.` pending, `~` active, `x` complete.

